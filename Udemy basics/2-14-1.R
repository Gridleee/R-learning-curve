?airmiles

head(airmiles)

tail(airmiles)

summary(mtcars)

summary(airmiles)

plot(mtcars)

plot(airmiles)

hist(airmiles)

head(mtcars)

attach(mtcars)

sum(mtcars$wt)

sum(wt)

detach(mtcars)

sum(wt)

mtcars[2, 6]

mtcars[c(2, 5, 8), 6]

